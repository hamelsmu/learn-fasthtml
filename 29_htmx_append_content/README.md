Complete the HTML to load and append new items to the list, removing the clicked button:

```html
<ul id="item-list">
  <!-- Existing items here -->
</ul>
<button hx-get="/more-items" [...]>Load More</button>
```